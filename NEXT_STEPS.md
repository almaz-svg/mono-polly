# План на следующий рабочий день — MarketWars

## Статус на сейчас
- Безопасность (Tier A) выкачена: RLS + хэшированные пароли через RPC, Grok-ключ на сервере, уникальные цвета команд.
- `supabase/001_rls_and_secrets.sql` и `002_team_color_unique.sql` нужно прогнать в Supabase, если ещё не прогнаны (первый — прогнан и починен после ошибки с pgcrypto; второй — проверить).
- Вход в `/admin` работает (admin / marketwars2024, либо новый пароль, если меняли).
- Устранён шум 406 в консоли (`.maybeSingle()` везде, где "последней строки" может не быть).

## 1. Обязательно проверить перед хакатоном (полный прогон вживую)
- [ ] Прогнать `supabase/002_team_color_unique.sql`, если ещё не прогоняли.
- [ ] Получить настоящий xAI-ключ на console.x.ai (текущий `GROK_API_KEY` — это ключ Groq, а не xAI, поэтому AI всегда молча уходит в локальный фолбэк). Обновить в `.env` и в Vercel → Environment Variables.
- [ ] Полный цикл вручную, с двух устройств:
  1. `/admin` → создать игру
  2. `/register` → зарегистрировать 2–3 тестовые команды, проверить что цвета нельзя повторить
  3. `/admin` → начать игру
  4. `/team/:id` → войти по паролю, сдать заявку (в т.ч. со скриншотом), проверить что ошибка (если будет) видна в форме
  5. `/admin` → завершить раунд → запустить AI-оценку → проверить, что оценка реальная (не всегда одно и то же число) и `ai_reason` осмысленный
  6. Расчёт акций → подтвердить → проверить `/dashboard` обновляется в реальном времени
  7. Довести до `game.status = finished`, проверить экран с медалями

## 2. Известные слабые места (Tier B, не сделано специально — обсуждали и отложили)
- Нет настоящей Supabase Auth-сессии: RLS не может отличить "своя команда пишет" от подделанного `team_id`. Технически подкованный участник может через devtools отправить заявку/оценку от имени чужой команды.
- Если решите закрывать и это — нужен отдельный проход: серверные API-роуты с токеном сессии вместо прямых записей в Supabase из клиента (акции, раунды, submissions, peer_scores).
- Решить: делать ли это до хакатона или жить с этим риском (маловероятно, что участники будут этим заниматься вместо самого хакатона).

## 3. Мелкий долг / cleanup
- `README.md` всё ещё документирует старый плейнтекст-пароль `admin/marketwars2024` и старый setup-скрипт с `DISABLE ROW LEVEL SECURITY` — стоит обновить, чтобы не сбивать с толку в будущем (и не хранить пароль в открытом виде в публичном репо — `mono-polly` на GitHub **публичный**).
- Storage-бакет `screenshots` — не проверяли политики доступа отдельно; сейчас не критично (не секрет), но стоит убедиться, что загрузка работает у реальных команд, а не только у тестовых.
- Bundle-warning "chunks larger than 500kB" — не критично для хакатона, можно проигнорировать.

## 4. Если будет время — не критично
- Показывать в админке, была ли оценка от реального Grok или от локального фолбэка (уже добавлено поле `source` в ответе `api/score.js`, просто не выводится в UI) — полезно для доверия к процессу во время игры.
- Разбить `Admin.jsx` (680+ строк) на подкомпоненты — чисто для поддерживаемости, не влияет на работу игры.
